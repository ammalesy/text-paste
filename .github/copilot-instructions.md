# TextPaste App

A Node.js/Express web app for recording text into timestamped server files, with a daily record viewer and auto-cleanup of files older than 2 days.

## Stack
- Runtime: Node.js
- Framework: Express
- Frontend: Vanilla HTML/CSS/JS (no build step)
- Storage: Local `.txt` files in `records/` directory
